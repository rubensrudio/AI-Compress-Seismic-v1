/**
 * JMH benchmark harness for AI-Compress-Seismic-v1.
 *
 * <p>This package contains the encode and decode benchmarks
 * ({@code SdcEncodeBenchmark}, {@code SdcDecodeBenchmark}),
 * the programmatic runner ({@code BenchmarkRunner}), and the
 * report generator ({@code BenchmarkReporter}).
 */
package com.sdc.bench;
